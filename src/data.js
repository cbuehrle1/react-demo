let buttonColors;
let buttonSpace = {

    buttonInitialize: function (count) {
      for (var i = 0; i < count; i++) {
        buttonColors[i] = 'red';
      }
    }

  };

export default buttonSpace;
